@*@model RTech.Demo.Areas.MockTest.ViewModels.ConversationsListVm


@{
    ViewBag.Title = "IncludedMockTests";
    Layout = "~/Views/Shared/_Layout_PTE.cshtml";
}

<!--  -->
<section class="uk-section-small uk-bg-dark-blue uk-light  uk-text-center uk-arrow-down uk-relative  ">

    <div class="uk-container  ">
        <h2 class="uk-margin-remove uk-text-bold">@Model.DialogueTitle</h2>
        <ul class="uk-breadcrumb uk-flex uk-flex-center">
            <li><a href="@Url.Action("Home","TopBar", new { @Area = "Student"})">Home</a></li>
            <li><span>Model.DialogueTitle </span></li>
        </ul>
    </div>

</section>
<!--  -->
<!--  -->
<!-- blog end-->
<section class="uk-section">

    <div class="uk-container ">
        <div class="uk-text-left" uk-grid>
            <div class="uk-width-expand@m">
                <div class="uk-db-header  uk-card-default ">
                    <h3> Mock Test</h3>
                </div>
                <div class="uk-card uk-card-default uk-card-body">
                    <div class="uk-child-width-1-2@s uk-grid-match uk-text-center" uk-grid>
                        <!--  -->
                        @{
                            foreach (var item in Model.Converstions)
                            {
                                <div>
                                    <div class="uk-mocktest-inlist uk-border-1 uk-padding">

                                        <h3 class="uk-margin-small"><a href="@Url.Action("PrepareForTest","PracticeTest", new { @Area="MockTest", @mockTestQuestionMasterId= item.Id})">@item.Title</a></h3>
                                        <a href="@Url.Action("PrepareForTest","PracticeTest", new { @Area="MockTest", @mockTestQuestionMasterId= item.Id})" class="uk-button uk-button-primary">Start Test</a>
                                    </div>
                                </div>
                            }
                        }


                        <!--  -->

                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!--  -->*@
